# A Python / Tornado Example

# Prerequisites

```
pip install pymongo flask psutil jsonpickle
```

The import process uses:
 * pymongo to access MongoDB
 * Flask to provide a HTTP status endpoint

# Running
python3 app.py
